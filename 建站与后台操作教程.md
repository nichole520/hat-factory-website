# Hat Factory 外贸网站 — 建站与后台操作教程

---

## 一、准备工作

### 1. 注册账号（全部免费）
- **GitHub**：https://github.com （用于存放网站代码）
- **Netlify**：https://www.netlify.com （用于托管网站，推荐）

### 2. 安装 Git（本地电脑）
- Windows/Mac 下载：https://git-scm.com/downloads
- 安装后打开终端，验证：`git --version`

---

## 二、把网站代码上传到 GitHub

### 1. 在 GitHub 创建仓库
1. 登录 GitHub，点击右上角 **+** → **New repository**
2. Repository name 填写：`hat-factory-website`
3. 选择 **Public**（公开）
4. 点击 **Create repository**

### 2. 推送本地代码到 GitHub
在电脑上打开终端，进入网站文件夹（`hats-website/`），依次执行：

```bash
cd hats-website
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/hat-factory-website.git
git push -u origin main
```

> 提示：把 `你的用户名` 替换为你的 GitHub 用户名。

---

## 三、部署网站到 Netlify（推荐，免费）

Netlify 与 Decap CMS 原生兼容，配置后台最简单。

### 1. 连接 GitHub 仓库
1. 登录 Netlify，点击 **Add new site** → **Import an existing project**
2. 选择 **GitHub**，授权 Netlify 访问你的仓库
3. 找到并选择 `hat-factory-website` 仓库

### 2. 配置构建设置
在配置页面填写：
- **Build command**：`hugo`
- **Publish directory**：`public`
- 点击 **Show advanced** → **New variable**，添加：
  - Key：`HUGO_VERSION`
  - Value：`0.123.7`

点击 **Deploy site**。等待 1-2 分钟，网站就会上线。Netlify 会分配一个免费域名（如 `https://xxx.netlify.app`）。

### 3. 开启 Netlify Identity（用于后台登录）
1. 进入 Netlify 站点后台 → **Identity**
2. 点击 **Enable Identity**
3. 进入 **Identity → Settings → Registration**
   - Registration preferences 选择 **Invite only**（防止陌生人注册）
4. 进入 **Identity → Settings → Git Gateway**
   - 点击 **Enable Git Gateway**
   - 会要求连接 GitHub，点击授权

### 4. 邀请自己为管理员
1. 进入 **Identity → Invites**
2. 输入你的邮箱，点击 **Send invite**
3. 查收邮件，点击链接设置密码

---

## 四、访问后台并编辑内容

### 1. 进入后台
在浏览器输入：
```
https://你的netlify域名/admin
```

点击 **Login with Netlify Identity**，输入邮箱和密码登录。

### 2. 后台界面说明
左侧菜单：
- **Pages**：编辑 Home、About、Contact 页面
- **News**：添加/编辑新闻
- **Products**：添加/编辑产品

### 3. 编辑首页（Home）
1. 点击左侧 **Pages** → **Home**
2. 修改 **Banner Title** 和 **Banner Subtitle**
3. 修改 **Categories**（7个分类）：
   - 点击分类项，修改 Name 和 URL
   - **Image**：点击上传图片，或拖拽图片到上传框
4. 点击右上角 **Publish** → **Publish now**
5. 等待 10-30 秒，网站自动更新

### 4. 编辑 About 页面
1. 点击左侧 **Pages** → **About**
2. 修改 **Tabs**：
   - **Company Profile / Culture**：修改 Content（支持富文本编辑）
   - **Factory Views**：
     - Type 选择 `gallery`
     - Images 中点击 **Add images** 添加图片
     - 填写 Caption（图片下方文字）
3. 点击 **Publish** 保存

### 5. 添加产品
1. 点击左侧 **Products**
2. 你会看到文件夹结构：
   ```
   products/
   ├── baseball-hat/
   │   ├── _index.md      ← 分类页标题
   │   ├── product-1.md
   │   └── product-2.md
   ├── bucket-hat/
   └── ...
   ```
3. 点击某个分类下的 **New Product**
4. 填写：
   - **Title**：产品名称（如 Baseball Hat 01）
   - **Image**：上传产品图片
   - **Body**：可选，填写产品描述
5. 点击 **Publish** 保存
6. 该产品会自动出现在对应分类页面中

> 提示：每个分类默认已有4个示例产品，你可以直接点击编辑或删除。

### 6. 添加新闻
1. 点击左侧 **News**
2. 点击 **New News**
3. 填写标题、发布日期、正文
4. 点击 **Publish** 保存

### 7. 图片上传说明
- 在 CMS 中任何 Image 字段，点击 **Choose different image** → **Upload**
- 支持直接从电脑选择图片（JPG、PNG、WebP）
- 上传的图片会自动存入 `static/images/` 文件夹并提交到 GitHub
- **不需要使用第三方图床**

---

## 五、在不同电脑上修改内容

因为所有内容都存储在 GitHub 仓库中，你可以：

1. **任何电脑打开浏览器**
2. 访问 `https://你的域名/admin`
3. 用 Netlify Identity 账号登录
4. 直接编辑内容，保存后自动发布

**完全不需要修改代码。**

---

## 六、绑定自己的域名

### 1. 购买域名
推荐：Namecheap、阿里云、腾讯云（约 50-100 元/年）

### 2. 在 Netlify 绑定域名
1. 进入 Netlify 站点后台 → **Domain management**
2. 点击 **Add custom domain**
3. 输入你购买的域名（如 `hatfactory.com`）
4. Netlify 会显示 DNS 配置信息
5. 登录你的域名服务商，添加对应的 DNS 记录（通常是 CNAME 或 A 记录）
6. 等待 5 分钟 - 48 小时生效

### 3. 启用 HTTPS
Netlify 会自动申请 Let's Encrypt 免费 SSL 证书，无需手动操作。

---

## 七、备选方案：GitHub Pages 托管

如果你坚持只用 GitHub Pages（不使用 Netlify）：

1. 进入 GitHub 仓库 → **Settings → Pages**
2. Source 选择 **Deploy from a branch**，Branch 选择 `main / (root)`
3. 保存后网站会部署到 `https://你的用户名.github.io/hat-factory-website/`

**注意**：GitHub Pages 下配置 Decap CMS 后台需要额外搭建 OAuth 代理，步骤较复杂。如需此方案，请告诉我，我可以单独补充教程。

---

## 八、常见问题

**Q：修改后网站没有更新？**
A：Netlify 自动部署可能需要 30 秒 - 2 分钟。也可以进入 Netlify 后台查看 Deploy log。

**Q：后台登录页面怎么找不到？**
A：前台网站没有任何入口链接。直接浏览器输入 `https://你的域名/admin` 即可。

**Q：想新增一个产品分类（第8个子菜单）？**
A：目前 CMS 界面不便直接创建新分类。需要：
1. 在 GitHub 仓库中进入 `content/products/`
2. 新建一个文件夹（如 `new-hat/`）
3. 在该文件夹内创建 `_index.md` 和若干产品 `.md` 文件
4. 同时在 `config.toml` 的 menu 中添加新的子菜单项

**Q：如何更换 Logo 文字？**
A：打开 `config.toml`，修改 `logoText = 'HAT FACTORY'` 为你想要的文字，提交到 GitHub。

---

## 九、项目文件说明

```
hats-website/
├── config.toml              ← 网站配置、导航菜单
├── content/                 ← 所有页面内容（CMS 编辑的就是这里）
│   ├── _index.md            ← 首页内容
│   ├── about.md             ← About 页面
│   ├── contact.md           ← Contact 页面
│   ├── news/                ← 新闻
│   └── products/            ← 产品分类及产品
├── layouts/                 ← 网页模板（一般不需修改）
├── static/                  ← 静态文件
│   ├── css/style.css        ← 样式表
│   └── admin/               ← 后台 CMS 入口
└── public/                  ← 生成的静态网站（自动构建，勿手动修改）
```

---

如有任何问题，随时告诉我！
